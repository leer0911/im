/**
 * @ Author: Lee
 * @ Description:
 * 用于存放所有消息类型，项目必须仅从这里获取消息类型
 */

export enum MESSAGE_TYPE {
  TEXT_SIMPLE, // 纯文本
  IMAGE_SIMPLE, // 纯图片
  SYSTEM_TEXT_SIMPLE, // 纯文本系统消息
}

export default MESSAGE_TYPE;
